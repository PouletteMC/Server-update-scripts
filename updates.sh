#!/bin/bash

hostname=$(hostname)

if [[ $hostname == quanta ]];
    then
    bash <(curl https://)

if [[ $hostname == asus ]];
    then 
    bash <(curl https://)

else 
    echo "Unknown computer"

fi